---
layout: base
---

<article>
  <header>
    <h1>{{ title }}</h1>
    <div class="post-meta">
      Posted
      <time datetime>{{ page.date | fromNow }}</time>
      by
      <span class="author">{{ site.author }}</span>
    </div>
  </header>
  {{ content }}
</article>
